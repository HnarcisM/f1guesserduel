{
  "name": "F1GuesserDuel",
  "version": "1.0.0",
  "main": "server/index.js",
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2",
    "helmet": "^8.2.0",
    "pg": "^8.22.0",
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "build:css": "node scripts/build-css-bundle.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "node --test --test-timeout=60000 test/e2e/*.test.js",
    "pretest:e2e": "node scripts/ensure-playwright-browser.js --strict",
    "test:e2e:install": "node scripts/ensure-playwright-browser.js --strict",
    "test:all": "npm test && npm run test:e2e",
    "e2e:install": "npm run test:e2e:install",
    "release:zip": "npm run build:css && node scripts/create-release-zip.js",
    "release:zip:with-tests": "npm run build:css && node scripts/create-release-zip.js --with-tests"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
